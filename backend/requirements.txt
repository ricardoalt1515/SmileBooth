# Core API (ligero)
fastapi
uvicorn
pydantic
python-multipart

# Imágenes (optimizado)
pillow
opencv-python-headless

# QR (ligero)
qrcode

# Database (solo lo necesario)
sqlalchemy

# macOS printing
pycups; sys_platform == 'darwin'
# Windows printing
pywin32; sys_platform == 'win32'

# Utils
python-dotenv
aiofiles
